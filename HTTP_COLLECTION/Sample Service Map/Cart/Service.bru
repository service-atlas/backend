meta {
  name: Service
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/services
  body: json
  auth: none
}

body:json {
  {
    "name": "Cart API",
    "type": "service",
    "description": "cart api",
    "url": "{{$randomUrl}}",
    "tier": 1
  }
}

script:post-response {
  if (res.status == 201) {
    bru.setEnvVar('cart_api_id', res.body.id, { persist: false})
  }
}
