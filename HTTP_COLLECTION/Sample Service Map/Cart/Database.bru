meta {
  name: Database
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/services
  body: json
  auth: none
}

body:json {
  {
    "name": "Cart Database",
    "type": "database",
    "description": "cart database",
    "criticality": 1
  }
}

script:post-response {
  if (res.status == 201) {
    bru.setEnvVar('cart_db_id', res.body.id, { persist: false})
  }
}
