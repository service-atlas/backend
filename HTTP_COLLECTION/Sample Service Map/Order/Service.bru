meta {
  name: Service
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/services
  body: json
  auth: none
}

body:json {
  {
    "name": "Order API",
    "type": "service",
    "description": "order api",
    "url": "{{$randomUrl}}",
    "tier": 1
  }
}

script:post-response {
  if (res.status == 201) {
    bru.setEnvVar('order_api_id', res.body.id, { persist: false})
  }
}
